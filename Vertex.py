import diff_treshold
import pandas as pd
import os
import re

class Vertex():
    ''' A class which represents a vertex in the graph.
    A Vertex will be implemeneted using a dictionary: 
    The key is the vertex name (trait name). The valus - list of traits with correlcation
    higher than treshold ordered by correlation'''
    
    def __init__(self):
        pass
    
    
    
    def __eq__(self, other):
        pass
    
    
    def __repr__(self):
        pass